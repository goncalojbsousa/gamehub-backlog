'use client';

import React from 'react';
import { GamePageSkeleton } from '@/src/components/skeleton';

const Loading: React.FC = () => {
    return <GamePageSkeleton />;
};

export default Loading; 